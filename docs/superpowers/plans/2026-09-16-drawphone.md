# 画画传话实施计划（2026-09-16）

Goal: 新增独立drawphone游戏，复用现有画板，不重建项目，不改变三国杀下架及数据保留策略。
Architecture: lib/drawphone.ts保存私密画册及每轮任务，rooms CAS推进阶段；drawing_boards沿用独立画作存储，扩展访问控制及提交锁定。共享画板增加保存状态回调。

- [x] 规则测试先行：3～6人，每人一本画册，随机固定顺序；每轮人人参与，文字/图画交替，每本经过所有人一次。测试不串册、只见上一条、超时草稿、重复/过期提交、房主逐条揭晓、反应表情。
- [x] 数据集成：newGame/act/playerView、catalog、rooms超时/CAS/无积分结束；drawing API精确校验当前作画任务、已提交不可修改，揭晓前拒绝非当前前序画作。原你画我猜保持可用。
- [x] 界面：写句子与灵感、文字草稿自动保存、画板与上传完成才可提交、等待人数、房主同步揭晓、自由翻阅、表情反馈；手机布局沿用。
- [x] 验证：单元测试、TypeScript、React渲染、3/4/6人HTTP全流程、你画我猜回归、生产构建。先隔离8792验证再更新8787；保留原数据库并核对三国杀摘要。

## Defaults
文字30字、45秒；绘画90秒。空白文字明确显示本轮未完成；图画通过画板内容判定空白。文本草稿本地即时保存、服务器防抖保存。完成锁定，全员完成立即下一轮。超时自动提交服务器已有草稿/画作。
揭晓从第1本第1条开始，只有房主可推进；结束后全部可自由翻阅，无胜负积分。表情限😂/神还原/怎么变成这样了；服务端限频并保留少量近期记录。房主可提前结束回准备阶段。

## 完成记录（2026-09-17）
62项规则测试、类型检查、React渲染、3/4/6人HTTP、真实45秒超时、原游戏HTTP回归通过。隔离生产构建通过并将同一构建复制至正式dist，8787沿用原数据库。更新前备份在outputs/drawphone-backup.json所列目录；三国杀两房间摘要保持一致。Tabbit启动失败，浏览器视觉和手机触控尚未验收。
